# Ensure optional dependencies (like @next/swc-linux-x64-gnu) are installed
optional=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Include all platform-specific binaries
ignore-scripts=false